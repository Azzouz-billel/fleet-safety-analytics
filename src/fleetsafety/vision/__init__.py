"""Phase 2 — vision events (detection, tracking, tailgating).

Intentionally empty until Phase 1 is validated on a real recorded trip.
The pipeline must keep working GPS-only when no video is present.
"""
